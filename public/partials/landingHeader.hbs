<style>
/* Global Styles */
body {
  background-color: black;
  margin: 0;
  font-family: "Roboto", sans-serif;
  font-weight: 100;
  font-style: normal;
}

/* Header Styles */
header {
  border:solid;
  border-bottom: white;
  position: fixed;
  top: 0;
  height: 70px;
  width: 100%;
  background-color: #280832;
  display: flex;
  z-index: 1000;
  padding: 0 20px; /* Padding to add space on the sides of the header */
  box-sizing: border-box; /* Ensures padding is included in width calculation */
}

/* Left Section (Logo) */
.header-left {
  flex: 1;
  display: flex;
  justify-content: flex-start; /* Horizontally center the content */
  align-items: center; /* Vertically center the content */
}
#logo-img {
  width: 200px;
  height: auto; /* Maintain aspect ratio */
  object-fit: contain; /* Ensure the image fits the container */
  transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth transition for hover effects */
}

#logo-img:hover {
  transform: scale(1.05); /* Slightly enlarge the logo */
  box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.3); /* Add a subtle shadow for a 3D effect */
  filter: brightness(1.2); /* Slightly increase brightness */
}


/* Middle Section (Navigation Links) */
.header-middle {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
}

.header-middle ul {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
  gap: 20px; /* Slightly larger gap for better spacing */
}


.header-middle li a {
  display: inline-block; /* Allows transform to work properly */
  padding: 10px 20px;
  border-radius: 50px;
  font-size: 15px;
  text-decoration: none;
  color: #fff;
  transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease, color 0.3s ease; /* Smooth transition */
}


.header-middle li a:hover {
  background-color: #83bce3;
  color: black;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
  transform: scale(1.05); /* Slightly enlarge the item */
}

/* Right Section (Sign In & Register Buttons) */
.header-right {
  flex: 1;
  display: flex;
  justify-content: flex-end;
  align-items: center;
}

.header-right ul {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
  gap: 10px;
}

.header-right li a {
  padding: 10px 20px;
  border: 1.5px solid #20D982;
  background-color: rgba(32, 217, 130, 0.1);
  border-radius: 50px;
  font-size: 15px;
  text-decoration: none;
  color: #fff;
  transition: background-color 0.3s ease, color 0.3s ease;
}

.header-right li a:hover {
  background-color: #20D982;
  color: black;
}

/* Logo */


/* Media Queries for Responsiveness */
@media (max-width: 768px) {
  .header-left {
    flex: none;
  }
  
  .header-middle {
    flex: 1;
  }

  .header-right {
    display: none; /* Hide the right section (Sign In/Register) on smaller screens */
  }

  .header-middle ul {
    gap: 0px; /* Smaller gap on smaller screens */
  }

  .header-middle li a {
    font-size: 14px; /* Slightly smaller text on smaller screens */
    margin: 1px; /* Reduce margin on smaller screens */
  }
  #logo-img {
  width: 170px;
}
}

</style>


<header>
  <div class="header-left">
    <a href="#" onclick="showHomePage()">
    <img id="logo-img" src="/images/new_logo_2.png" alt="Money Mind Logo">
    </a>
  </div>
 
  <div class="header-middle">
    <ul>
      <li><a href="#" onclick="showHomePage()">Home</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#services">Services</a></li>
    </ul>
  </div>

  <div class="header-right">
    <ul>
      <li><a href="#" onclick="showSignInPage()">Sign In</a></li>
      <li><a href="#" onclick="showRegisterPage()">Register</a></li>
    </ul>
  </div>  
</header>

