<style>
body,html {
  background-color: black;
  margin: 0;
  font-family: "Roboto", sans-serif;
  font-weight: 100;
  font-style: normal;
}
header {
  position: fixed;
  top: 0;
  height: 70px;
  width: 100%;
  background-color: #280832;
  display: flex;
  z-index: 1000;
  padding: 10px; /* Padding to add space on the sides of the header */
  box-sizing: border-box; /* Ensures padding is included in width calculation */
}
.header-left {
  flex: 0.1;
  display: flex;
  justify-content: flex-start; /* Horizontally center the content */
  align-items: center; /* Vertically center the content */
}
#logo-img {

  width: 170px;
  height: auto; /* Maintain aspect ratio */
  object-fit: contain; /* Ensure the image fits the container */
  transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth transition for hover effects */
}

#logo-img:hover {
  transform: scale(1.05); /* Slightly enlarge the logo */
  box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.3); /* Add a subtle shadow for a 3D effect */
  filter: brightness(1.2); /* Slightly increase brightness */
}


.header-right,.header-middle {

    flex: 0.45;
    display: flex;
    justify-content: flex-end;
    align-items: center;
}
.header-right ul,.header-middle ul {
  display: flex;
  justify-content: space-around;
  gap:20px;
  list-style: none;
}

.header-right li a,
.header-middle li a {
  display: inline-block; /* Allows transform to work properly */
  padding: 10px 20px;
  border-radius: 50px;
  font-size: 15px;
  text-decoration: none;
  color: #fff;
  transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease, color 0.3s ease; /* Smooth transition */
}

.header-right li a:hover,
.header-middle li a:hover {
  background-color: #83bce3;
  color: black;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
  transform: scale(1.05); /* Slightly enlarge the item */
}



#profileButton:hover #menu-img {
  content: url('images/menu.jpg'); /* New image on hover */
}

#profile {
  position: fixed;
  top: 80px;
  right: 10px;
  width: 150px;
   background-color: white;
  padding: 10px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  text-align: left;
}

  .profile-info {
  cursor: pointer;
  font-size: 17px;
}
.profile-info:hover {
  text-decoration: underline;
}


@media (max-width: 768px) {
  #logo-img{
    width:200px;
  }

  header {
    flex-direction: column;
    height: 120px;
    }
    .logobar{
      flex: 1;
      display: flex;
      justify-content: center;
    }
     .header-right {
        height:30px;
        flex: 1;
        display: flex;
        justify-content: center;
    } 
    .header-right ul li a{
      font-size: 15px;
    
    }
    #menu-img{
      width: 20px;
    }
#profile {
  position: relative;
  top: 120px;
  width: 100%;
  background-color: white;
  border-radius: 0px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  text-align: center;
}

.profile-info {
  padding: 5px;
  margin: 0px;
  cursor: pointer;
  font-size: 16px;
}

}

</style>
<header>
    <div class="header-left">
      <a href='/home'>
      <img id="logo-img" src="/images/new_logo_2.png" alt="Money Mind Logo">
      </a>
    </div>
    <div class="header-middle">
      <ul>
        <li id="transaction-link"><a href='/fetch-transactons'>Transactions</a></li>
        <li id="home-link"><a href='/home'>Overview</a></li>
        <li id="add-link"><a href='/add-transaction'>add</a></li>
      </ul>
    </div>
</header>


        <div id="profile" style="display: none;">
            {{!-- <p class="profile-info">My GPTs</p>
            <p class="profile-info">Customize ChatGPT</p>
            <p class="profile-info">Settings</p>
             --}}
            <p class="profile-info">Upgrade Plan</p>
            <div id="logout-div">
              <div id="logout-img" style="display:inline">
                <img src="/images/logout.png" width="17px">
              </div>
              <p style="display:inline;" class="profile-info" onclick="window.location.href='/auth/logout' ">Log out</p>
            </div>
        </div>

<script>
    function showProfile() {
        const profileDiv = document.getElementById('profile');
        console.log(profileDiv.style.display);
        profileDiv.style.display = profileDiv.style.display === 'none' ? 'block' : 'none';
    }

    document.addEventListener("DOMContentLoaded", () => {
        // Prevent body click from hiding profile when clicking inside the profile menu
        const profileDiv = document.getElementById('profile');
        if (profileDiv) {
            profileDiv.addEventListener("click", (event) => {
                event.stopPropagation();
            });
        }

        // Prevent body click from hiding profile when clicking the profile button
        const profileButton = document.getElementById("profileButton");
        if (profileButton) {
            profileButton.addEventListener("click", (event) => {
                event.stopPropagation();
            });
        }

        // Add a body click listener to hide the profile menu
        document.body.addEventListener("click", () => {
            console.log("body clicked");
            if (profileDiv && profileDiv.style.display === 'block') {
                profileDiv.style.display = 'none';
            }
        });
    });
</script>
